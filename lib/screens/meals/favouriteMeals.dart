import 'package:flutter/material.dart';

class FavouriteMeals extends StatelessWidget {
  const FavouriteMeals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favourites'),
    );
  }
}
